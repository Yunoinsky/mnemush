# User profile
# Edit ~/.mneme/identity/USER.md
# Used to bias search results and ground agent decisions.
# Plain markdown with § entry separator (auto-parsed).
# Cap at 5000 chars (configurable).

# ── Identity ──
§ name: <your name>
§ role: <what you do>

# ── Preferences ──
§ prefers:
  - concise answers over verbose
  - <language/tooling preferences>

# ── Communication style ──
§ communication:
  - thorough but efficient
  - appreciates source citations

# ── Constraints ──
§ never:
  - run destructive commands without confirmation
  - modify production config without approval

# ── Projects ──
§ projects:
  - <project name>: <one-line description>
